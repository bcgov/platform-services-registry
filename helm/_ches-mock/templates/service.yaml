apiVersion: v1
kind: Service
metadata:
  name: {{ include "_ches-mock.fullname" . }}
  labels:
    {{- include "_ches-mock.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "_ches-mock.selectorLabels" . | nindent 4 }}
