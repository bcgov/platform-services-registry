apiVersion: v1
kind: Service
metadata:
  name: {{ include "_mailpit.fullname" . }}
  labels:
    {{- include "_mailpit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "_mailpit.selectorLabels" . | nindent 4 }}
