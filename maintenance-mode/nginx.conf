user nonrootuser;
pid /tmp/nginx.pid;  # redirect pid

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        listen 7070;
        server_name _;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        error_page 404 /index.html;
    }
}
