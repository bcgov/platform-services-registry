policy:
  approval:
    - or:
      - one-reviewer
      - auto-merge

approval_rules:
- name: one-reviewer
  requires:
    count: 1
    users:
    - "w8896699"
    - "okanji"
    - "Kolezhanchik"
- name: auto-merge
  description: Automatic merge with auto-merge label
  if:
    has_labels:
      - "auto-merge"
  requires:
    count: 0
