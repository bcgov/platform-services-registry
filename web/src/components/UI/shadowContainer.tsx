import styled from '@emotion/styled';
import { Box, Flex } from 'rebass';


const boxShadow = `box-shadow: 1px 3px 4px 4px #cbcbcb7d`;

export const ShadowBox = styled(Box)`
  ${boxShadow};
`
export const ShadowFlex = styled(Flex)`
  ${boxShadow};
`
